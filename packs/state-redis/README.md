# State Redis Pack

Persistent key-value state provider backed by Redis.

## Pack ID
- `state-redis`

## Components
- `state-provider-redis`

## Secrets
- `redis_url` (tenant): Redis connection URL (e.g. redis://127.0.0.1:6379).

## Flows
- `setup_default`
- `update`
- `remove`
